import React from 'react';

// Auth pages use the same canonical client application as the workspace.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
